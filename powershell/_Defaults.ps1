


if(-not $exePath) { $exePath = "..\target\debug\rstproto.exe" }
if(-not $logLevel) { $logLevel = "Info" }
if(-not $dataDir) { $dataDir = "..\data\" }
if(-not $actor) { $actor = "threddyrex.org" }


